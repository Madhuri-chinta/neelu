---
parameters:
- name: pool
  displayName: pool
  type: string
  default: sweety
- name: verbose
  displayName: verbose
  type: boolean
  default: true  

steps:
- task: Npm@1
  inputs:
    command: 'install'
    verbose: "${{ parameters.verbose }}"
    publishPackageMetadata: true


